services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: dnd_forge
      POSTGRES_USER: dnd_user
      POSTGRES_PASSWORD: changeme
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
